# Backlog

- Windows installer/MSI signing and update channel.
- User-authorized Sol -> frontend and Sol -> engineer runs. The App Server
  evidence ledger is implemented, but acceptance still requires real completed
  delegations; fixtures do not count.
- Browser/desktop QA matrix with automated screenshots on Windows. Computer Use
  currently refuses the Codex Orchestra window until the user approves that app.
- Explicitly confirmed live compatibility and agent-behavior checks for Qwen,
  Kimi/OpenCode Go and Grok OAuth.
- Long-running idle/leak profile against the documented memory budget.
- Optional embedded plugin UI if Codex later adds a first-party panel host. The current format is skills + MCP + desktop deeplinks.
