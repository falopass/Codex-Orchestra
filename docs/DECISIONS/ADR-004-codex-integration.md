# ADR-004: Codex integration strategy

Status: accepted

The primary integration is generated agents + a private skill + a managed `AGENTS.md` block. App Server stdio is optional and not a prerequisite for router configuration.
