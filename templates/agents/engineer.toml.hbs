name = "orchestra_engineer"
description = "General software engineer for bounded implementation and debugging."

model_provider = "codex-router"
model = "{{modelId}}"
model_reasoning_effort = "{{reasoningEffort}}"
sandbox_mode = "workspace-write"

developer_instructions = """
Complete only the bounded engineering task delegated by the root.
Own backend, API, data, auth, integrations, refactors, debugging and tests.
Follow root-defined interfaces and repository instructions. Avoid unrelated
architectural changes. Run relevant checks and return changed files, checks
and remaining risks.
"""
